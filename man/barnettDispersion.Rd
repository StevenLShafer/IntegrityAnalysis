% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dispersionTest.R
\name{barnettDispersion}
\alias{barnettDispersion}
\title{Barnett's dispersion test for one trial, evaluated by quadrature}
\usage{
barnettDispersion(tstats, prior = 0.5, slabVar = .bdSlabVar, points = 513L)
}
\arguments{
\item{tstats}{a data frame from \code{\link[=barnettTStats]{barnettTStats()}}, or any data frame
with numeric columns \code{t} (the two-sample t-statistic for one table
row and one pair of arms) and \code{df} (its degrees of freedom).}

\item{prior}{prior probability of the slab. 0.5, the reference default.}

\item{slabVar}{prior variance of \code{epsilon}. 10, the reference value.}

\item{points}{grid points for the quadrature; must be odd. 513 agrees
with an 8,193-point grid to better than 1e-9 on every quantity
returned, which the tests pin.}
}
\value{
a one-row data frame:
\describe{
\item{\code{nStat}}{number of t-statistics used.}
\item{\code{pDispersed}}{posterior probability of the slab - Barnett's
trial-specific probability. His threshold for flagging is 0.95.}
\item{\code{epsilon}}{posterior mean of \code{epsilon} GIVEN the slab.
Positive is under-dispersion, negative over-dispersion.}
\item{\code{gamma}}{\code{exp(epsilon)}, the multiplier on the precision. A
gamma of 2 means the differences between arms are tighter than
sampling predicts by a factor of \code{sqrt(2)} in standard
deviation.}
\item{\code{direction}}{"under-dispersed", "over-dispersed" or "none".}
\item{\code{multiplier}, \code{multiplierLo}, \code{multiplierHi}}{the reference
app's reported multiplier and its 5-95\% interval, computed from
the MIXTURE posterior (slab draws and prior draws together) so
that these three columns are directly comparable with what his
Shiny app prints. When \code{pDispersed} is low the mixture is mostly
prior, so \code{multiplier} heads for 1 while its interval stays
enormous; that is a property of his summary rather than a defect
here, and it is why \code{epsilon} is reported separately above.}
}
}
\description{
Computes the posterior probability that a baseline table is under- or
over-dispersed under the spike-and-slab model of Barnett
(F1000Research 2022, 11:783), together with the estimated dispersion
multiplier. See the file header for the model, and for why this is a
quadrature rather than an MCMC.
}
\seealso{
\code{\link[=barnettTStats]{barnettTStats()}} to build the input from a validated frame.
}
