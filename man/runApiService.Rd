% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apiService.R
\name{runApiService}
\alias{runApiService}
\title{Run the IntegrityAnalysis REST service}
\usage{
runApiService(port = 8080, host = "0.0.0.0")
}
\arguments{
\item{port}{TCP port (default 8080, the AWS App Runner convention).}

\item{host}{bind address; \code{"0.0.0.0"} for containers.}
}
\value{
Called for the side effect of running the server; blocks.
}
\description{
Starts the plumber API defined in \code{inst/api/plumber.R}. Endpoints:
\code{GET /health} (open); \code{POST /parse} and \code{POST /analyze} (bearer
token). Uploads are multipart (\code{file}); an \code{X-Anthropic-Key} header
turns on the per-request AI assist under the caller's own key
(issue 8's service side). Every upload is deleted when its request
ends, and the response says so - the retention contract of issue 1.
}
