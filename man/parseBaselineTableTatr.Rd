% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parseTatr.R
\name{parseBaselineTableTatr}
\alias{parseBaselineTableTatr}
\title{Parse a baseline table using Table Transformer geometry}
\usage{
parseBaselineTableTatr(
  pdfFile,
  tatrXml,
  trial = tools::file_path_sans_ext(basename(pdfFile)),
  parenIsSD = c("auto", "sd", "percent"),
  roundObsDelta = 1,
  maxCandidates = 6,
  pctApprox = FALSE,
  ocr = c("auto", "never", "always"),
  ocrDpi = 300,
  quiet = FALSE
)
}
\arguments{
\item{pdfFile}{path to the PDF.}

\item{tatrXml}{path to the Table Transformer XML for that PDF.}

\item{trial}{Value for the TRIAL column. Defaults to the PDF file name.}

\item{parenIsSD}{How to read "a (b)" cells that carry no percent sign:
\code{"auto"} decides from the footnotes ("mean (SD)"), the row label
("n (\%)"), and continuous-variable keywords (age, weight, ...);
\code{"sd"} always reads mean (SD); \code{"percent"} always reads n (\%).}

\item{roundObsDelta}{ROUND_OBSERVATION is set to ROUND_MEAN +
\code{roundObsDelta}. Observations are often recorded with one more digit than
the reported mean, hence the default of 1. Set to 0 to make them equal.}

\item{maxCandidates}{How many captioned tables to attempt before giving up.
Candidates are tried in order of caption score.}

\item{pctApprox}{Opt in to APPROXIMATE percent conversion. Percent-block
cells are normally converted to counts only when the printed percentage
and the arm N pin exactly one integer; with \code{pctApprox = TRUE}, cells
the bracket cannot pin fall back to \verb{round(N x pct / 100)}. Every such
value is recorded in \verb{$approxCounts} and \verb{$derivedCells} and reported
by \code{\link[=reviewFlags]{reviewFlags()}} - it is a computed approximation (off by up to half
a printed unit of N/100), not a printed datum. Default \code{FALSE}.}

\item{ocr}{\code{"auto"} reads pages with no text layer through tesseract,
\code{"always"} reads every table's page through tesseract, \code{"never"}
uses only the text the XML carries.}

\item{ocrDpi}{Rendering resolution for OCR. Higher is slower and not
necessarily better; 300 is a reasonable default for journal scans.}

\item{quiet}{Suppress the progress and summary messages.}
}
\value{
A \code{ParsePDFTable}, as \code{\link[=parseBaselineTableHeuristics]{parseBaselineTableHeuristics()}} returns
it, with \code{layout = "tatr"} and \code{engine} \code{"heuristic-tatr"} or, when
the winning table was read by OCR, \code{"heuristic-tatr-ocr"} (rows then
carry \code{"ocr"} provenance and the app shades them cyan).
}
\description{
The Table Transformer counterpart of \code{\link[=parseBaselineTableHeuristics]{parseBaselineTableHeuristics()}}:
the table's rows and columns come from the model's detections (a
\verb{*.tatr.xml} written by \code{python/tatr/tatrTables.py}), the characters
from the PDF's text layer or, on a page with none, from local OCR
(tesseract). Every table the model found is a candidate; the engine's
own caption and parse scoring choose the winner. Deterministic and
offline.
}
