# Screen adjudication log

One line per finding, newest first. Raw reports are local and
gitignored (see [README](README.md)); this is the public record of what
was found and what was decided.

| date | finding | severity | outcome |
|---|---|---|---|
| 2026-09-08 | **F1 (screen 1048)** — the zero snap's tolerance, `1e-26 * (1 + centre^2)`, is a property of the coordinate system, not of the statistic it thresholds: quadratic in an arbitrary origin, with an absolute floor at small magnitudes, while the statistic it is compared against is translation-invariant and scales as k². An author choosing units or an offset scale moves the tolerance independently of the data | medium (the answer stops depending on the data) | **fixed** — reproduced on this machine first: three arms of 1,000 with SD 0.001 and means differing by 2e-5, 3e-5 and 5e-5 read p = 0.3105, 0.573 and 0.897 at origin 0 and **0.492 for all three** at origin 1e9, because the tolerance (1e-8 there) swallowed the observed statistic and nearly every replicate. Nothing refused those rows. `.iaZeroSnapTol(dd, h)` now scales to the TRANSLATED deviations, so it inherits the statistic's own invariances, with the finest printed grid step as a floor. The screen's own proposal (`1e-12 * max(dd^2)` alone) was measured and rejected: it is exactly zero when the arms agree, which removes the dust guard in the one case it exists for, and moved a pinned value in `test-sd-rounding-draw.R` by 0.013 — a replicate is translated by the OBSERVED first arm, so its weighted centre of equal drawn values is not bitwise equal to them. After the fix the same row reads 0.312 at origin 1e9 against 0.3105 at origin 0. Tests pin translation invariance, scale covariance, the non-zero floor and the end-to-end reproduction; all four fail on the previous code. |
| 2026-09-08 | **F1 (screen 0709)** — the fine-precision Note fired on ordinary honest rows. A spreadsheet stores `45.0` as the double 45, so `.iaDecimals()` reports 0 decimals for that arm; `shownMin` becomes 0, `round(c(45.2, 45.0, 44.8), 0)` is all-equal, and the editor was told the arms "print alike" when the page shows them differing in the first decimal. The inferred `ROUND_MEAN` is the row maximum, so the suppression gate could never fire either | low-medium (false disclosure; dilution of the only remedy for this shape; a lever an author could spray) | **fixed** — reproduced first: `45.2 / 45.0 / 44.8` at `ROUND_MEAN` 1 carried the note end to end. The equality branch now also requires the arms to lie within one step of the FINEST printed precision, the same "within one step, or the claim did not decide the answer" gate the coarse note already applied; the 1e-3 is dust allowance, checked against the pinned 1e-13 perturbations. The wording changes from "the arms print alike" to "the arms are equal at the digits they print", because the other branch admits arms the engine judged equal by its own snap. Every case the screen tabulated is pinned, including the two that used to fire wrongly. |
| 2026-09-08 | **F3 (screen 0709)** — `.iaCapRegistry()` kept the tail, so a last file in a zip yielding thousands of ordinary derived cells evicted the OCR provenance paint of every earlier file. Keeping the head, which this replaced, had the mirror defect; both ends are reachable through a zip, so the previous fix traded one exposure for the other | low (display integrity; the numbers are unaffected) | **fixed** — the cap is now by PRIORITY: `ocr`, `ai`, `failsafe` and `recovered` rows are kept while any plain `derived` row remains, and registry order is preserved because the payload resolves a shared cell last-entry-wins. Pinned with the screen's own construction — 9,001 rows whose FIRST entry is the OCR row, capped to 100, asserting it survives — which fails on the previous code. |
| 2026-09-08 | **F4 (screen 0709)** — the disclosure note's zero tolerance and the engine's were the same expression written twice, six lines apart, in each of two branches. They agreed, but nothing made them agree, and the tolerance had already been changed three times on the record | low (hardening; it is the exact regression the commit existed to prevent) | **fixed, and the formula fixed first** — screen 1048's F1 was right that binding a wrong formula once is not enough. `.iaZeroSnapTol()` is now the single definition and both branches take `zeroTol = zt` from one binding. Tripwire property group 7 asserts it statically: no simRow may set `zeroTol` from a literal except the categorical branch's legitimate `0`, `.iaZeroSnapTol()` is defined once and called exactly twice, and the origin-dependent expression may not return. Verified to pass on the fix and fail on the previous code. |
| 2026-09-08 | **S1 (screen 0709, suspected and unverified there)** — `.iaDerivedPayload()` and `.iaSkipPayload()` matched grid rows to registry rows by pasting TRIAL and ROW with a carriage return between. Both fields are document text, so a field carrying the separator makes the decomposition ambiguous: TRIAL "A" with ROW "B⏎C" and TRIAL "A⏎B" with ROW "C" produce the same key, and one entry could claim a grid row it does not name — in the wrong order replacing an OCR warning with a benign derived note | low | **fixed without waiting for a reachable path.** I could not construct a parser that emits a carriage return into those fields either, so this is the shape closed rather than a demonstrated exploit — but a user types into the grid, and the fix costs nothing. `.iaCellKey()` length-prefixes each field, so no string can forge another string's key whatever the separator; NA is encoded as a value and does not collide with the literal "NA". Pinned with the screen's collision and with a payload test asserting a forged entry paints nothing; both fail on the previous code. |
| 2026-09-08 | **F2 (screen 0709)** — `ROUND_OBSERVATION` was removed from the coarse-precision note rather than gated, so a median row's p inflation from a coarse observation grid is undisclosed again | medium (an undisclosed false negative) | **accepted and left open, deliberately.** The removal was itself the adjudication of screen 2339's F2, where the note fired on honest rows including alarming ones and asserted that a large p rested on a claim that had moved it by nothing. Re-adding it behind an effective-step gate is the right shape, but the mechanism it would describe is the observation grid's effect on the MEDIAN branch, and that branch has no dispersion-side consistency test at all — the open decision Steve Shafer is holding pending a corpus measurement. Fixing half of it now would disclose a claim we cannot yet check. Recorded here so it is not lost, and named in the audit brief so an outside reviewer is asked about it directly. |
| 2026-09-08 | **F5 (screen 0709)** — residual: `.iaDerivedPayload()`'s whole-trial pass still scans the grid per entry and grows four vectors with `c()` | low, and already adjudicated as the accepted residual of screen 2339's F3 | **accepted, no change, and the two premises the bound rests on are now written down** — trial identifiers are disambiguated per file, so a `"*"` entry matches only its own file's rows and the painted total stays `nrow(d) × ncol(d)`; and the number of `"*"` entries is the number of OCR-read or arm-N-recovered FILES, not the registry cap. If either changes — a path that registers `"*"` per row, or trial names colliding across files — the cost becomes the product again with only the registry capped. The screen's separate observation that nothing caps `nrow(d)` at upload is real and pre-existing; it is a grid-size bound, not this finding, and is not taken here. |
| 2026-09-08 | **F2 (screen 1048)** — the uncommitted 2026-09-08 audit report and its evidence publish unadjudicated evasion recipes to a public repository, which is the same reason `docs/security-screens/screen-*.md` is gitignored | informational (a disclosure policy call) | **decided by Steve Shafer: commit it.** "It shows diligence on our part." Committed verbatim with its evidence in PR #227, after checking the report and all 18 evidence files for key, token, password and private-key material, home-directory paths and the user's email address (none), and confirming the synthetic fixture PDF carries no `/JavaScript`, `/OpenAction`, `/Launch`, `/AA`, `/EmbeddedFile` or `/RichMedia`. The two findings it exposes are both queued or fixed: F2 by PR #228, F1 by this commit. |
| 2026-09-03 | **second screen of PR #168 (53a01f6..b5b5611, the fix commit)** — no findings. Verified: reachability only inside the timed child behind `.ppImageOK` (app, API and engine); the decode is bounded at 5 megapixels by the header check (about 160 MB of doubles for a 16-bit RGBA PNG, the accepted ISSUES 32 residual); header and decoder agree on size for every PNG/JPEG divergence constructed; BMP header arithmetic inside 32 bits; the BMP dies with the request directory; no attacker-controllable writer failure | — | **clean; two hardening notes applied** the same evening: `.ppImageGrey()` enforces the pixel cap itself (a pin asserts it has one caller, `.ppImageUpscaled()`, verified to trip), and the BMP is written in the child's own temporary directory rather than beside the upload. Pre-existing residual noted, not new to this diff: libpng's inflated ancillary chunks (zTXt/iTXt/iCCP) under the child's missing memory ceiling — ISSUES 32's `ulimit -v` wrapper remains the fix |
| 2026-09-03 | **F1 (screen of PR #168, screenshot enlargement)** — `.ppImageUpscaled()` decoded the whole picture (png/jpeg into double-precision arrays) BEFORE judging the enlargement factor against the pixel cap, so an 88 KB blank 5000 x 4000 PNG cost the child 1,088 MB to decode for an enlargement the cap then refused; the ordinary 5-megapixel screenshot at factor two peaked at 765 MB through the double-precision replication | medium | **confirmed → fixed.** The factor is decided from the header (`.ppImageDims`) and the function returns before any decode when the enlargement would pass the cap; `.ppImageGrey()` turns the decoded pixels into one byte each at once, so the replication and the BMP are raw vectors (a 5-megapixel picture at factor two is 20 MB of pixels, not 153 MB of doubles plus temporaries). Test: an over-cap picture never reaches the decoder (spy). Two group-6 pins: the decoders are called only inside `.ppImageGrey()`, and `.ppImageDims` precedes `.ppImageGrey` inside `.ppImageUpscaled()` — both verified to trip |
| 2026-09-03 | **F2 (screen of PR #168)** — the BMP writer made one `writeBin` call per pixel row (4.5 us each), so a 1 x 5,000,000 PNG, under the cap, had ten million rows at factor two and spent the whole 60 s child budget in the loop | low | **confirmed → fixed.** The pixel block is built with vector operations (rows reversed by indexing, each byte tripled with `rep(each = 3)`, padding inserted by a matrix reshape) and written once. Test: a 5 x 3 matrix with one byte of padding writes 102 bytes with the bottom row first |
| 2026-09-03 | **F3 (screen of PR #168)** — AGENTS.md's standing conclusion still said pictures are decoded only by tesseract's leptonica reader, and tripwire group 6 knew nothing of the two new decoders | documentation | **confirmed → fixed.** The bullet now names libpng and libjpeg through the `png` and `jpeg` packages, the single decoder site, and the header-before-decode order; group 6 pins both (F1) |
| 2026-09-03 | note (screen of PR #168) — verified: the gate order holds (`.ppImageOK` before `.ppImageData` in the engine, repeated by the API before the child); the format is chosen from magic bytes, never the name; the BMP is our own bytes and is removed on the normal path; no new code execution; the lockfile adds only `png` 0.1-9 and `jpeg` 0.1-11 from CRAN | — | a second screen covers the fix commit |
| 2026-09-03 | **fifth screen of PR #162 (92ba352..727b231, the fourth round's fixes)** — no findings. Verified: the NUL and first-byte checks are exactly the closure of the UTF-16/EBCDIC bypass (UTF-16LE `<` is `3C 00`, caught by the NUL scan; EBCDIC's first byte is 0x4C, refused by the first-byte check; every ASCII-superset encoding keeps `<!ENTITY` as ASCII bytes for the existing search); the libxml2 field returns the running version; the four count bounds act before the work; the cell-count XPath matches the cells the matrix build iterates; the adapter pin reads comment-stripped source. Measured at the 8 MiB gate: 2,000,000 empty `<p/>` 7.9 s (none kept), 800,000 packed cells 3.9 s (table skipped) | — | **clean.** Two residuals accepted: (1) `xml_find_all` materialises the nodeset before the caps act, bounded by the 8 MiB gate to under 10 s, linear not super-linear; (2) the deferred adapter's ceiling is about five tables of 20,000 cells, roughly 50 s under `load_all` and less byte-compiled, inside the 60 s child timeout that bounds it regardless. #162 merges on this screen |
| 2026-09-03 | **F1 (fourth screen of PR #162)** — the `<!ENTITY` gate searched ASCII bytes, so a UTF-16 file (BOM, every letter followed by a NUL) declaring a 100 KB entity referenced 400 times passed the gate; libxml2 decodes the document from its byte-order mark, DTD subset and all. On this build its own amplification cap still refused it in the child; on the image's older libxml2 (rocker 4.5.3 ships the 2.9 line, whose entity accounting is unverified) the exposure is the third screen's F2 again, and the gate existed to be independent of that version | medium | **confirmed → fixed.** Two byte checks in `.ppJatsOK()`, both version-independent and refusing nothing real: a NUL anywhere (not a legal character in any XML encoding; 0 of the corpus's 10,108 JATS files hold one) and a first byte of `<` after optional whitespace and a UTF-8 BOM (which also closes EBCDIC, where `<` is 0x4C). Test: the same entity document refused as UTF-8, shown to pass the raw search as UTF-16, refused as NUL; BOM + whitespace accepted; `MZ<article/>` and an all-whitespace file refused. Pinned in group 1b (two pins, both verified to trip) |
| 2026-09-03 | **F2 (fourth screen)** — `/health` reported `libxml2: null` on every reply: xml2 1.5.2 does not export `libxml2_version()`, the exported call threw, the `tryCatch` swallowed it. The third screen's log claimed the field "now reports the running libxml2 version"; it did not, and nothing tested it | low | **confirmed → fixed** (87cb86a, found the same hour by the R client on PR #163 printing the reply). Reached through `utils::getFromNamespace`; the health test asserts a version string. The same shape as 2026-08-27 F2: a stated guarantee the code did not deliver, caught only when something read the reply |
| 2026-09-03 | **F3 (fourth screen)** — pre-existing, verified by timing: a table at the row and column caps (2,000 x 200 = 400,000 cells, 3.8 MiB) spent 90 s building the matrix and 212 s in the word adapter; 1.5 million empty `<p/>` spent 179 s in the paragraph loop with the text budget untouched; and the adapter ran for all 100 table-wraps before any was ranked, so nested wrappers sharing one set of rows multiplied the cost by their depth (a depth-10 probe did not finish in ten minutes). The child is killed at 60 s and the request gets a 422, but the single plumber thread is blocked for the minute | low | **confirmed → fixed, by counts rather than time.** Cells are counted by libxml2 before any is built: 20,000 per table (a real baseline table has under 2,000) and 100,000 per document across table-wraps; the body loop iterates at most 20,000 outermost paragraphs; and the adapter now runs lazily inside the candidate loop, for the tables actually tried, with the "no usable table content" message preserved. Each turns a 60 s pin into a millisecond refusal. Tests: the 30,000-cell table skipped in under 5 s beside a real table that still parses; 40 tables of 5,000 cells stop at the document budget; 60,000 empty paragraphs under 10 s. Four pins (cells, document cells, paragraphs, adapter placement), all verified to trip |
| 2026-09-03 | note (fourth screen) — verified: the caption XPath's disjoint outermost subtrees bound the join by the file size; the incremental text budget is applied; the row cap keeps column order; the tripwire pins run on comment-stripped source; the temp path is substituted by the upload name before any message leaves the API; the `.xml` route still runs in the child with `ai = "never"` and the 60 s timeout; no NOENT or DTDLOAD, so F1 does not reopen XXE | — | the fifth screen covers the F1–F3 fix commits and 87cb86a (plumber.R is a watched file) |
| 2026-09-03 | **F1 (third screen of PR #162)** — the caption XPath guarded `<p>` but not `<title>`: 250 nested `<title>` around a 4 MB leaf materialised 1 GB before the clip, and the per-node clip did not bound the joined caption | medium (verified) | **fixed.** One XPath selects outermost `<p>` or `<title>` (`not(ancestor::p) and not(ancestor::title)`), at most five caption nodes are read, and the joined caption is clipped. Test: 250 nested titles around a 1 MB leaf, seconds and bounded; tripwire: every `<title>` selection carries the guard, verified to trip |
| 2026-09-03 | **F2 (third screen)** — every clip acts after `xml_text()` has built a node's text; on this libxml2 (2.12) an entity referenced many times inside one node is capped at parse time, on the API image's 2.9 line that accounting was reworked later and is unverified | low here, suspected medium on the image | **fixed independently of the libxml2 version.** A declared internal entity (`<!ENTITY`) is refused by name in `.ppJatsOK()` — none of the corpus's 10,108 real JATS files declares one — and the text budget is applied as paragraphs are read, not after all are built; `/health` now reports the running libxml2 version. Tests: the refusal by name; tripwire pins both, verified to trip |
| 2026-09-03 | **F3 (third screen)** — a row of 200 cells of 500 words each became a 99,504-word synthetic line and spent the child budget in the tokenizer; pre-existing on the Word route | low | **fixed**: a row is capped at 500 words in the shared adapter; test pins it. The tokenizer's per-token data frame stays as it is (an ordinary slow parse, bounded by the timeout) |
| 2026-09-03 | note (third screen) — verified: the body, footnote and row guards; nested table-wraps bounded by the cap; `.ppClip()` bytes-versus-characters slack bounded; the one-word line; error text carries no path; the bytes route, gzip and size gates and the AI-off rule unchanged | — | recorded |
| 2026-09-03 | **F1 (second screen of PR #162, the fix commits)** — the JATS extractor selected paragraphs and rows on the DESCENDANT axis, so a `<p>` nested 250 deep (libxml2 allows 256) put every ancestor's text, each containing the leaf, into the text vector: a 5 MB file under every gate became 1.25 GB of characters and a 3.8 GB child; an internal entity referenced five times inside the nesting stacked a further multiplier. The Word parser walks direct children and is not affected | medium (verified) | **fixed.** Only outermost nodes are selected (body paragraphs, caption paragraphs, footnotes, rows - an outer node's text already holds the inner, so real articles read the same); every cell (2,000 chars), paragraph (20,000) is clipped; the text vector has a 2 MB budget. Tests: 250 nested paragraphs around a 3 MB leaf, the entity variant, nested rows in a cell - each in seconds and bounded. Tripwire pins the guards and the applied budget, verified to trip |
| 2026-09-03 | **F2 (second screen)** — the shared word adapter built one data frame per word, so a single 5 MB cell of one-character words spent the whole child budget; pre-existing on the Word route, inherited by JATS | low | **fixed.** The adapter clips a cell, caps a line at 500 words, and builds each line from vectors in one call; test: 2.5 million words in under five seconds, and a real cell lands its words at the same places as before |
| 2026-09-03 | note (second screen) — verified: the bytes route does not inflate a gzip; the 8 MiB ceiling's residual DOM cost is about half a gigabyte (ISSUES 32); a wall of 640,000 empty table-wraps costs 8 s and 350 MB before the cap; the clamps, the parent-side gate and the new tripwire assertions do what they say; error text carries no path; the AI route stays off for XML | — | recorded |
| 2026-09-03 | **F1 (screen of PR #162, JATS XML through the API)** — a gzip stream named `.xml` passed the 25 MiB request cap on its compressed size and libxml2's FILE reader inflated it transparently: a 389 KB upload became 400 MB of XML and a parse child past 15 GB inside the 60 s budget; ISSUES 32 (no child memory ceiling) reachable from a sub-megabyte request. The app path had the same exposure since #129 | **high** (verified) | **fixed, three ways, each pinned.** The reader parses BYTES (libxml2's memory parser never inflates); `.ppJatsOK()` refuses the gzip magic by name and an on-disk ceiling of 8 MiB (the corpus's 10,108 real JATS files: median 99 KB, 99th percentile 239 KB, largest 3.5 MB); the API runs the same gate before spending a child (422 with the reason). Tests: a 40 MB-of-XML gzip refused in under a second, an oversize file refused, the API's 422. Tripwire: exactly one `read_xml()`, on bytes, NOBLANKS alone; the gate before the read - all verified to trip |
| 2026-09-03 | **F2 (screen)** — `colspan="100000000"` in a 300-byte table made a 1.5 GB matrix and a 4.5 GB child in ten seconds; every `<table-wrap>` is materialised before candidates are ranked; the Word parser's gridSpan has the same shape | medium (verified for JATS) | **fixed.** Spans clamped to 50 in both parsers, a row wider than 200 columns makes the table not one the engine reads, 2,000 rows kept per table, 100 table-wraps per document. Tests pin the span, the width and the wall of tables; tripwire pins both clamps, verified to trip |
| 2026-09-03 | **F3 (screen)** — the tripwire banned HUGE, NOENT, DTDLOAD but not DTDVALID (an external-entity read, measured), DTDATTR (fetches the external DTD - a server-side request to an author's URL) or XINCLUDE | low | **fixed.** All three banned; the reader's single call pinned positively; the source-level test in test-parse-jats.R names all six |
| 2026-09-03 | note (screen) — verified non-findings on this route: billion laughs refused under the current options; external entities and DTDs not fetched under NOBLANKS; the AI route forced off for `.xml` so content cannot reach a model; child error text rewrites the tempdir path to the upload name; extension dispatch consistent and case-insensitive; output paths unchanged | — | recorded |
| 2026-09-03 | **screen of #152–#158 (38b50ac..1dac412), with `inst/www/` and `app.R` added to the watched list** — Steve: drag-and-drop and paste "open up the potential security risks". Drop and paste: **clean** — the page handler only forwards files to the existing input, the server re-applies its own allowlist, every name reaches the log escaped, a pasted picture meets the same header preflight. Build-commit shim: the SHA is accepted only if it matches a hex pattern and comes from the deploy's own head. Workflows: apt additions only, still `pull_request`. Unicode splitter measured linear | — | **two low findings elsewhere in the range, fixed; two notes** |
| 2026-09-03 | **F1 (screen)** — the OCR rescue aimed the engine at the image pages but the engine rendered and OCRed EVERY page at 300 dpi (`pages` only filtered candidates afterwards): a 30-page preprint spent the whole child budget on pages the text layer had read, and the rescue failed on the documents it was built for — live in production since #158 | low (verified) | **fixed.** `.ppOcrPagesAt()` renders only the requested pages and places each at its page index (the renderer names its output by page); test with a three-page document asserts one page rendered |
| 2026-09-03 | **F2 (screen)** — on a Table Transformer host the rescue chain asked the seam twice (on the text engine's error, then inside the OCR rescue) and the parse was not memoised, so a degraded scan was rendered and OCRed through the model twice before plain OCR ran a third time | low (verified from code) | **fixed.** `tatrParse()` memoises its result per OCR mode, NULL included; test asserts one model parse across the chain |
| 2026-09-03 | **N1 (screen)** — ISSUES 32 (no memory ceiling on the parse child) has moved from latent to live now that tesseract deploys; each page bounded to a few hundred MB by the caps, against a 1 GB shinyapps instance | informational | **accepted, open under ISSUES 32** — instance size or a child-level limit, Steve's call |
| 2026-09-03 | **N2 (screen)** — the page-size cap skipped itself when `pdf_pagesize()` errored (fail-open); the short-count case was checked against pdftools source and cannot occur | informational | **fixed anyway**: a document whose page sizes cannot be read is not rendered at all; test pins no render on a page-size error |
| 2026-09-03 | **screen of #145's adjudication commits (872fdc6..38b50ac)** — no findings; the screen drove the changed TIFF parser with crafted files (two-page bomb, count-2 LONG, BYTE-typed, big-endian, 11 x 4096 entries in 0.27 s) and traced every preflight-before-decode order | — | **clean; three notes recorded below** |
| 2026-09-03 | note (screen) — the 20 MP cap bounds pixels, not decoded bytes: 16-bit four-sample images decode to several bytes per pixel before leptonica's conversion, so a cap-passing image may allocate a few hundred MB in the child | informational | **accepted, open under ISSUES 32** (the child memory ceiling closes it for every format at once; folding bit depth into the preflight is the alternative) |
| 2026-09-03 | note (screen) — the F2 tripwire is a negative grep (`pages == 1L` absent) and would not catch a first-directory-only rewrite phrased differently | informational | **accepted; comment added** naming the two-page test as the real pin |
| 2026-09-03 | note (screen) — after `.ppImageAiRefusal()` a TIFF cannot reach the type switch in `.ppImageFileB64()`, so its "Only JPEG and PNG" stop is dead code | informational | **accepted, left as belt-and-braces** |
| 2026-09-03 | **F1 (re-screen of #145's fix commits, 88e18e0..872fdc6)** — a TIFF ImageWidth/ImageLength entry with a count above 1, or a type other than SHORT/LONG, carries an OFFSET in its value field; the parser read it as a small dimension, so a crafted first directory could pass the cap while the real dimensions sat elsewhere. libtiff's own behaviour on such an entry unverified from this machine | low (suspected) | **fixed rather than argued.** Such an entry now refuses the file outright (`return(NULL)`), which also removes the `max(NA)` warning a BYTE-typed entry raised in the API's parent. Tests pin a count-2 LONG width and a BYTE width; tripwire group 6 pins the refusal and was verified to trip on its removal |
| 2026-09-03 | **F2 (re-screen)** — the pixel cap read the FIRST TIFF directory only, so a 10 x 10 page 1 ahead of a 60000 x 60000 page 2 passed; whether a decoder reads past page 0 was not verified from source | low (suspected) | **fixed.** Every directory's dimensions are read and the largest is what the cap sees, whichever page a decoder picks; the property no longer depends on leptonica. Test pins the two-page case refused; tripwire pins that `pages == 1L` does not return |
| 2026-09-03 | **N1 (re-screen)** — `parseBaselineTable(ai = "always")` reached `.ppImageFileB64()` with the 20 MP preflight but without the model's own 4.5 MB / 8000 px limits, which lived only on the fallback path; unreachable from the app and the API (both pass `"fallback"` or `"never"`), console callers only | informational | **fixed anyway, cheaply.** `.ppImageFileB64()` now calls `.ppImageAiRefusal()` before reading a byte, so the guarantee lives in the function that reads the bytes; test pins an 8500 x 2000 PNG refused; tripwire pins the call order |
| 2026-09-03 | **N2 (re-screen)** — two comments still named GIF among the accepted formats | documentation | **fixed.** Comments corrected; the tripwire's GIF assertion reads the token table and was never masked by them |
| 2026-09-03 | note (re-screen) — everything else in the fix range checked and found sound: every decode path gated by the header preflight, format from bytes not names, bounded header parsing in every branch, the zip route and the retry queue, the token-table tripwire, model output still untrusted. Two earlier runs of this screen died on an API overload and wrote no findings — and advanced the ledger anyway, which is PR #148 | — | recorded |
| 2026-09-02 | **F1 (screen, image uploads)** — GIF: the preflight read the logical screen, but giflib sizes every frame from its own image descriptor, so a 1 x 1 screen could precede a 65535 x 65535 frame and pass every header check; the child has a wall-clock timeout and no memory ceiling | **high** | **confirmed → fixed by removal.** Verified from the giflib/leptonica sources the screen cited. Steve's rule for GIF and PNG was "unless they are free"; a block-stream walk would have made GIF not free, and giflib's overflow guard is the known-ineffective `Width < 0 && Height < 0`. GIF is out of `.ppImageExts`, out of the UI, zip, API and guide, and tripwire group 6 refuses its return. PNG stays: IHDR IS the dimension its decoder uses. The pixel cap was also lowered 50 → 20 MP (the screen's note). The memory-ceiling recommendation is **accepted, open** — it belongs to the container (App Runner / shinyapps.io limits, or `ulimit` in the Linux launcher) and cannot be verified from the Windows development machine; filed as ISSUES.md issue 32 |
| 2026-09-02 | **F2 (screen)** — a PNG signature with a NUL among bytes 13-16 made `rawToChar()` throw inside `.ppImageDims`, so the API returned the fixed 500 instead of a 422 with a reason | low | **confirmed → fixed.** Raw-byte comparison; a test pins the NUL case returning NULL |
| 2026-09-02 | **F3 (screen)** — a repeated ImageWidth/ImageLength tag in a TIFF directory: the loop kept the LAST value, and which occurrence libtiff honours is unverified, so `256 = 60000` then `256 = 10` could pass the cap | low (suspected) | **confirmed as a cap bypass → fixed.** The parser now keeps the LARGER value of a repeated tag, so the cap sees the worst case whichever the decoder picks; a test pins 60000-then-10 refused |
| 2026-09-02 | **F4 (screen)** — tripwire group 6 grepped raw source, so a comment naming `.ppImageOK(` above a moved `.ppImageData(` call would satisfy the order test — the failure mode AGENTS.md itself records | low | **confirmed → fixed.** Group 6 now strips comments before every grep, and the assertion was verified to FAIL on a scratch copy with the calls swapped and exactly such a comment left behind |
| 2026-09-02 | note (screen) — the scanned-PDF OCR route renders at 300 dpi with no page-size cap, so a PDF declaring a 200 x 200 inch MediaBox would render at 60000 x 60000; pre-existing, not touched by the image diff | — | **accepted, open.** Recorded with the memory ceiling in ISSUES.md issue 32; the new image cap makes the asymmetry visible |
| 2026-09-02 (TATR seam, PR #147) | **F1 (screen)** — no memory ceiling and no page-dimension cap ahead of two rasterisers: the model's `render()` and the OCR route's `pdf_convert` would both rasterise a 200 x 200 inch MediaBox (30,000 px a side at 150 dpi; 3.6 GP at 300), and the OS timeout bounds time, not memory | medium (latent: no deployed image has the Python) | **confirmed → fixed.** Both rasterisers refuse pages over 30 inches on a side or 20 megapixels at their dpi (`MAX_PAGE_INCHES`/`MAX_PAGE_PIXELS` in tatrTables.py; `.ppRasterMaxPixels` in `.ppOcrPages`), and `.ppTatrRun` passes `--max-mem-mb`, which sets `RLIMIT_AS` on the Python (Linux). A 200-inch synthetic page is pinned skipped on the R side. The R parse child itself still has no ceiling — that remains ISSUES 32 |
| 2026-09-02 (TATR seam) | **F2 (screen)** — the runner discovered `~/tatrenv` and the script under cwd/home, so a host provisioned for corpus work would serve uploads through pdfium, pdfminer and a 1 GB torch process with nobody deciding so; AGENTS.md claimed the opposite | low | **confirmed → fixed.** Configuration only: `INTEGRITY_TATR_PYTHON` and `INTEGRITY_TATR_SCRIPT` (or `INTEGRITY_ROOT`); tripwire trips on any `tatrenv`, `path.expand` or `getwd()` in the discovery functions; AGENTS.md corrected |
| 2026-09-02 (TATR seam) | **F3 (screen)** — the runner's 600 s budget exceeded the parse child's 60 s, so a slow model run consumed the whole child and took the OCR rescue with it; `assign_words` is quadratic in a hostile page's word count | low | **confirmed → fixed.** The batcher exports `INTEGRITY_PARSE_BUDGET`; the runner takes at most half of it and a timeout returns NULL, so the seam falls through to plain OCR; `page_words` ignores a page over 5,000 words. Pinned by a fake-runner test with a 4 s budget and a 6 s run |
| 2026-09-02 (TATR seam) | **F4 (screen)** — a child SIGKILLed at the timeout never runs its `on.exit`, so its tempdir survived holding page renders of the manuscript (pre-existing) and now the model's XML of every cell | low | **confirmed → fixed.** The parent creates the child's TMPDIR/TMP/TEMP and removes it after the call whatever the status; a test asserts no `child*` directory survives a run |
| 2026-09-02 (TATR seam) | **F5 (screen)** — a comma in the uploaded filename truncated the list-file path and silently disabled the seam through the API | informational | **confirmed → fixed.** The upload is copied into the work directory as `upload.pdf` before the list is written; the fake-runner test uploads `Table 1, revised.pdf` |
| 2026-08-29 (later) | the **42.8% corroboration figure was itself measured with the stale 0.1.0 parser**. `measureMisparse.R` loaded the snapshot library with `library(pkg, lib.loc = dir)`, which does NOT put `dir` on `.libPaths()`; `parseBaselineTableFiles()` hands each parse subprocess the parent's `.libPaths()`, so every child resolved `IntegrityAnalysis` from the renv project library — where the installed version is **0.1.0**. The script printed `engine: version 0.2.0` truthfully of the parent and of nothing that read a PDF | **high** | **confirmed → fixed (#115).** The 2026-08-29 entry below replaced one route to the stale build with another. Verified directly: on the desktop, `.libPaths()` under renv resolves `IntegrityAnalysis` to 0.1.0. Remeasured on a dedicated Linux node where the snapshot library is the ONLY copy on the system, so the failure is unavailable rather than merely avoided: **42.8% → 44.8% fully corroborated**, parsed 988 → 1,047. The defect's practical effect was modest; the provenance claim was false. Lesson: the development machine is the worst place to verify library provenance, because its second copy hides exactly this |
| 2026-08-29 | **F1 (screen)** — a label column named `ROWS` (or anything matching `grep("ROW")` but not equal to it) zeroed the categorical compute term: `validateData` renames it, the gate matched exactly | **high** | **confirmed → fixed.** VERIFIED: `drawWork` 1.9e10 → 0, refused → accepted, and the table analysed anyway (~2 h CPU for a 180 KB upload). Root cause was mine: the 2026-08-28 fix added a SECOND normalizer matching a subset of `validateData`'s rules. Now one shared `.iaNormalizeNames` |
| 2026-08-29 | **F2 (screen)** — `NUMBER` and `N` both present: `validateData` renames `NUMBER`→`N` unconditionally, leaving two columns named `N`; R's `$` takes the first, so the gate read N=1 while P_Calc got N=5000 | **high** | **confirmed → fixed.** Gate scored 24× under budget for work 208× over. Shared normalizer performs the rename unconditionally, and `validateData` now REFUSES duplicate normalized names rather than silently picking one |
| 2026-08-29 | **F3 (screen)** — the copied `MEASURE`→`ROW` rename omitted `validateData`'s coupled `GROUP`/`DECSD` drops, so a Carlisle-2016 file the APP accepts got a 422 from the API with six bogus cell issues | medium | **confirmed → fixed.** Self-inflicted by the 2026-08-28 commit. The drops travel with the rename in the shared function; my comment claiming `validateData` "is idempotent under this" was simply wrong |
| 2026-08-29 | **F4 (screen)** — `DATA$N` partial-matches, so a `Notes` column made `is.null(DATA$N)` FALSE, skipped the structural failure, and crashed at `DATA[i, c("N","MEAN","SD")]` — an uncaught error in the app's reactive | medium | **confirmed → fixed.** Pre-existing, but the new ceiling added two more consumers of the same idiom. Now `DATA[["N"]]` throughout |
| 2026-08-29 | **F5 (screen)** — the app has NO compute gate at all, and the new ceiling reads `DATA$N`, which is NA on every categorical row by construction | medium | **accepted, open.** Pre-existing and real: the ceiling bounds one of three `P_Calc` branches while the guide now says the limit applies to "IntegrityAnalysis". Filed rather than patched — it needs the app-side gate the API has, not a wider ceiling |
| 2026-08-29 | corroboration figure regenerated from a clean snapshot (0.2.0 / 9e85d0a) after the original used `load_all` on the live tree AND a stale 0.1.0 build | — | **44.4% → 42.8% fully corroborated**, zero-corroboration 10.5% → 13.5%. The old number FLATTERED. A third defect found while checking: the script counted 101 files that extracted NOTHING as "fully corroborated", inflating its own headline to 48.7% |
| 2026-08-27 | **F1 (screen)** — claimed `.apiJournalCells` under-bounds by a `maxArms` factor | — | **rejected, with measurement.** `nrow(DATA)` already carries arm multiplicity. Measured actual/estimate against `buildBaselineTables`: 1.50× at 2 arms, 1.20× at 5, 1.10× at 10, **1.02× at 50** — the ratio *falls* toward 1 as arms grow, the opposite of the claim. The residual is the constant `Variable` column. Comment corrected to state what the estimator counts and the measured 1.5× worst case; threshold unchanged (a bounded 1.5× against a 200,000 cap is a few MB, not an explosion) |
| 2026-08-27 | **F2 (screen)** — `.apiAnalyze` builds `journalTablesOmitted` but `inst/api/plumber.R` never returns it, so a caller sees `journalTables: null` with no reason | medium | **confirmed → fixed in follow-up.** Notable: PR #91's own commit message claimed callers "read a reason instead of guessing at a null". They did not. The screen caught a gap between a stated guarantee and the code |
| 2026-08-27 | **F3 (screen)** — the "arm names become column headers" rationale survives in `securityCheck.R:151,170` and a test comment, though headers are positional literals (`"Arm 1 (n = 15)"`, baselineTable.R:127) | documentation | **confirmed → fixed in follow-up.** The same overstatement was corrected in PR #91's commit message but never chased into the comments it justified |
| 2026-08-27 | **F4 (screen)** — `tooBig` gates rows, trials, N and columns independently with `\|\|`, so 5,000 rows x 200 trials x N=100,000 passes every gate while the Monte Carlo cost is their product | **high** (raised from medium after measuring) | **confirmed → queued.** Measured draw rate 1.01e8/sec. A SINGLE row at the permitted maximum (N=100,000, 100,000 replicates) costs **199 seconds** — already past App Runner's request timeout on a single-threaded service. The full gate maxima come to **1.0e14 draws ≈ 12 days of compute in one request**. Reachable, not theoretical: escalation to 100,000 replicates triggers on *homogeneous* rows, which the submitter controls by making arm means near-identical. Same class as the journal amplification — every gate passes, the product is catastrophic. **Fixed** in PR #93: `.apiDrawWork` / `.apiMaxDrawBudget` bound the worst case before validation; over budget the request is refused and told to split by trial. Refusal vs. a *disclosed* replicate reduction was put to Steve as a statistical rather than security judgment; his decision 2026-08-27 — "keep the refusal, a coarser p-value is worse than a refusal" — is recorded in the code so it is not reopened as a performance tweak |
| 2026-08-27 | **F5 (screen)** — the app-side journal download (`app_server.R:1390`) calls `buildBaselineTables` with no bound | low | **accepted for now.** The uploader is spending their own session, and grid size is bounded by what the parser emits or a person types. shinyapps.io sessions do share an instance, so this is not zero — queued behind F4 rather than dismissed |
| 2026-08-27 | `journalTables` expands super-linearly in the input, so the `/analyze` input gates did not bound the response — a legal table becomes a multi-hundred-MB build on a single-threaded service | high | **fixed** — `.apiMaxJournalCells` bounds the estimated output before anything is built; pinned by tripwire group 5 and a direct test of `.apiJournalCells` |
| 2026-08-27 | `.apiCsvSafe` sanitized cell values but not column names | low (defense in depth — headers are positional literals, *not* a live exploit) | **fixed** — names sanitized; tripwire assertion anchored `^[^#]*` after the first version passed on a commented-out line |
| 2026-08-27 | AGENTS.md still claimed the AI fallback is off in deployment and manuscript text never reaches an LLM — false since the bring-your-own-key assist (issue 8, PR #67) | documentation | **fixed** — corrected, and model output is now stated to be untrusted input |
| 2026-09-05 | **F1 (screen 1758)** — `?seed=` in a link lets whoever wrote the link choose the draw; the log line presented the seed only as a reproducibility feature | low | **fixed** — the app's log line names the seed's source ("taken from this page's address") and says that a seed supplied by someone else lets them choose the draw, with the remedy (remove `?seed=`, analyze again). The Summary-sheet row already disclosed the seed. |
| 2026-09-05 | **F2 (screen 1758)** — the 0.1 escalation raises the typical `/analyze` cost about tenfold; the comments describing typical cost (`P_Calc.R`, `apiService.R`) were stale | low | **fixed** (comments) — the three comments now describe the 0.1 rule and its typical cost (a 25-row honest trial advances to 10,000 about 93% of the time). The budget gate was set from the worst case, which is unchanged, so no gate change. |
| 2026-09-05 | **F3 (screen 1758)** — seeding is process-global and persists after the request or session | informational | **accepted** — verified by the screen: no security-relevant consumer shares R's user RNG (Shiny ids use a private seed, API tokens use `openssl::rand_bytes`, `tempfile()` does not draw from it), and only a token holder can set the process seed. The residual is a determinism property, not an exposure. |
| 2026-09-05 | **F4 (screen 1758)** — `is.null(DATA$MEAN)` and `is.null(DATA$SD)` partial-match, so a sheet with `MEANX`/`SDX` and no `MEAN`/`SD` crashed the uploader's session (pre-existing, outside the diff; the same defect fixed for N on 2026-08-29) | low | **fixed** — `DATA[["MEAN"]]` and `DATA[["SD"]]`, with a test uploading an `SDX` column and no `SD` and expecting a structural refusal rather than an error. |
| 2026-09-05 | (found while verifying #179 on the deployed service, not by the screen) the API's `seed` form field never arrived: a multipart text part without a `Content-Type` reaches plumber as an empty list, so every seeded request was refused 422 | defect | **fixed** — the seed travels on the URL (`?seed=N`) in both clients and the guide; the service recognises the dropped part and its 422 says what to send instead. |
| 2026-09-05 | **F1 (screen 2117)** — the CSV column gate counted the first line only, while `read.csv(header = FALSE)` sizes the frame from lines 1–5: a two-field first line followed by four lines of 50,000 commas built a 10,001 × 50,001 frame past the gate (verified by the screen) | high | **fixed** — `.iaCsvColumns` is the maximum field count over every line (`count.fields` on the whole file, linear in an upload the size cap bounds); test with a narrow first line and wide lines 2–5. |
| 2026-09-05 | **F2 (screen 2117)** — the app called `parseWideTable` before its own decompression preflight (which #184 had put only in `readSheet`), and the xlsx reader re-inflates the archive once per sheet on both surfaces | high | **fixed** — the preflight now lives inside `.wideRawCells`, so every caller of the wide reader is behind it; a workbook with more than 10 sheets is refused before any sheet is read (`.iaSheetCountCap`). |
| 2026-09-05 | **F3 (screen 2117)** — libxls parses a whole `.xls` sheet and allocates its declared grid before `n_max` applies; a few-KB file declaring row 65,535 × 256 cells costs ~800 MB per read, and #184 read each sheet twice (a zero-row column preflight, then the bounded read) | medium | **mitigated, decision pending** — one read per sheet now (the column cap is judged on its result), halving the cost. The screen's fuller remedies — drop `.xls` (ask editors to save as `.xlsx`) or read it in the parse subprocess under its timeout — are a product decision for Steve. |
| 2026-09-05 | **F4 (screen 2117)** — the paste interceptor tested a class the bundled Handsontable does not use (its clipboard element is `id="HandsontableCopyPaste"`, class `copyPaste`, on `document.body`), and a paste into an OPEN cell editor was prevented and then lost | low | **fixed** — the element is matched by id and class; when the editor is open the plain text is inserted into the editor at the cursor instead of re-dispatched. Verified in headless Chrome: header, grid paste and editor paste all blocked, the editor shows the pasted text. |
| 2026-09-05 | **F5 (screen 2117)** — an unbalanced quote on a CSV's first line made `count.fields` return NA, which errored inside `if` (caught by the callers' tryCatch, so a refusal, not a crash) | informational | **fixed** — NA refuses explicitly (`.iaCsvTooWide`), with a test. |
| 2026-09-06 | **F1 (screen 0514)** — a count of ~1e308 in a long-layout level column was never swept for magnitude, summed to Inf, and the arm ceiling's `is.finite` guard let it through to the engine: a dead session in the app, a 500 on the API | medium | **fixed** — the Inf/magnitude sweep now covers every numeric column outside the base set (category and level counts); an overflowed arm total is over the ceiling; the Analyze observer wraps `P_Calc` and `.apiAnalyze` wraps `validateData` and `P_Calc`, so an unforeseen engine error is a message / a 422 naming the stage. Tests for the 1e308 level count and the stubbed engine error. |
| 2026-09-06 | **F2 (screen 0514)** — `is_category` compared with `as.integer`, which is NA beyond 2^31 − 1 or at Inf, and the `if` upstream crashed (a 500 on the API; the app's new tryCatch caught it with an unhelpful message) | low | **fixed** — `all(is.finite(x) & x %% 1 == 0)`; the sweep paints the cell first. Test with 3e9 and Inf. |
| 2026-09-06 | **F3 (screen 0514)** — the ±20 rounding clamp skipped a rounding column that arrived as text (one stray text cell makes the whole column character), and `ROUND_DISPERSION` was never coerced | low | **fixed** — all three rounding columns are coerced to numeric before the clamp. Test with "99" and "x"/"Inf". |
| 2026-09-06 | **I1–I3 (screen 0514)** — the 400 mapping keys on "subscript out of bounds" text; `.apiScrubPath`'s `name` argument is unused; the sanity filter matches the literal `%00` only | informational | **accepted** — with `validateData` and `P_Calc` now wrapped, no internal error with that text can reach the error handler from the analysis path; the unused argument is harmless; the "embedded nul" branch of the error handler backstops any other NUL encoding. |
| 2026-09-06 | **F1 (screen 0617)** — the ±20 rounding clamp ran before the alias renames, so a column named `ROUND`, `OBSERVATION` or "MEAN DECIMALS" carried 2e9 decimals to the journal-table formatter, which spent up to 50 s formatting an 8 KB string before failing (results and journal downloads; a 200 with `journalTables: null` on the API) | medium | **fixed** — the clamp now runs after the renames and the NA fill, on all three rounding columns; `.fmtAt` bounds its digits to 20 as defense in depth. Tests for the three alias names and the formatter bound. |
| 2026-09-06 | **I1 (screen 0617)** — the API's 422 for an unforeseen validation or engine error carries the R condition text | informational | **accepted** — a deliberate departure from the fixed-500 rule for the two wrapped calls, recorded here; the screen verified neither touches the file system or the environment. |
| 2026-09-06 | **I2 (screen 0617)** — double HTML escaping in the two new message handlers (`<b>` showed as `&lt;b&gt;`) | cosmetic | **fixed** — the inner escapes dropped; `outputComments()` escapes. |
| 2026-09-06 | **I1 (screen 0703)** — the decimal bump could raise a rounding value above the ±20 clamp: a mean of 1e-300 inferred 300 decimals | informational | **fixed** — `.iaDecimals()` is capped at 20; test with 1e-300. |
| 2026-09-06 | **I2 (screen 0703)** — a rejected rounding value (21, Inf) is silently replaced by the inferred one | informational | **accepted** — the replacement is the documented inference for a blank cell, and the value it replaces is one no table prints; painting it would add a colour for a case no real upload produces. |
| 2026-09-06 | **I1 (screen 0814)** — the per-row PowerPoint slide judged its "<" label against the first stage's 1,000 draws while the row's p came from its final stage, understating an alarming row | informational (cosmetic) | **fixed** — the collector carries the row's display string and final replicate count; the slide prints them verbatim and says the histogram is the first stage's draws. No security finding in the range. |
| 2026-09-06 | **I1 (screen 0933)** — the per-row slide's subtitle read "p from 1e+05": `format()` of a double 100000 | informational (cosmetic) | **fixed** — the count is coerced to integer before formatting ("p from 100,000"). No security finding in the range. |
| 2026-09-06 | **Screen 1024** (the #193 range) — no security finding; one informational test-coverage note on the changed slide line | informational | **accepted** — the line coerces a trusted count; the graph tests cover the slide. |
| 2026-09-06 | **Repeat outside screen, F1** — plumber's native request cap was unset (0), so a body was buffered before the size filter could refuse it | high (if nothing upstream caps it) | **fixed** — `runApiService()` sets `plumber.maxRequestSize` to the filter's 25 MiB before building the router; httpuv refuses an oversized declared length 413 with nothing buffered. Verified on a local service. The proxy/WAF cap in front of App Runner remains the belt for the braces. |
| 2026-09-06 | **Repeat outside screen, F2** — the Word route had no archive preflight (a 17 KB .docx declaring 4 MiB was read) and its table matrix was allocated before any bound on rows × spanned columns (15 KB of XML made a 101 × 5,000 matrix) | high | **fixed** — `.ppDocxData()` runs the spreadsheet route's decompression preflight before `officer` unpacks the archive; `.ppDocxTableMatrix()` applies the JATS caps (rows, columns after spans, cells, tables per document) before allocating. Tests for both. |
| 2026-09-06 | **Repeat outside screen, F3** — the AI route's page renderer had no page-geometry gate (local OCR did), so a 4 KB PDF declaring 200 × 200 inch pages reached `pdf_convert` at 150 dpi | high when the AI route is on | **fixed** — one gate, `.ppRenderablePages()`, fails closed and is used by both rasterisers. Test with a 200-inch page. |
| 2026-09-06 | **Repeat outside screen, F4** — the public app has no total work budget (100 median rows at 5,000 per arm pass validation) | medium | **accepted, open** — Steve's standing decision not to reduce Monte Carlo precision silently; a queue with cancellation or a preflight refusal is the remedy when a shared-worker incident makes it worth its cost (ISSUES.md issue 32). |
| 2026-09-06 | **Repeat outside screen, A1** — the scheduled screen's watched list omitted `R/parseJats.R` and `R/P_Calc.R` | assurance | **fixed** — both added, with the screen's own tools; the empty JATS XXE test block now asserts its outcome. |
| 2026-09-06 | **F1 (broad screen 1118, e9b710f..e561fb9)** — `count.fields` treats a line opening with `#` as a comment and `read.csv` does not, so a wide line starting with `#` was invisible to the CSV column gate and built by the reader (10,001 × 3,001 from 62 KB) | high | **fixed** — `comment.char = ""` in the gate, so it counts exactly what the reader reads; tests with the `#` line on line 1 and on lines 2–5. |
| 2026-09-06 | **F2 (broad screen 1118)** — the sigma draw's `rep(sig, N)` added a third N × chunk vector at the full-simulation peak (1.6 GB → 2.4 GB on the worst 5,000-per-arm row) | medium | **fixed** — standard normals scaled by column recycling (`matrix(rnorm(N * ch), nrow = ch) * sig + meansim`), bit-identical under a seed (the known-answer pins are unchanged), peak back to 1.6 GB. |
| 2026-09-06 | **F3 (broad screen 1118)** — `.apiScrubPath` removed the request directory from reasons but not the parse child's tempdir, a sibling under the parent's | informational | **fixed** — the parent's tempdir is scrubbed as well, longest match first; test. |
| 2026-09-06 | **Screen 1523** (the #195 range, e561fb9..166dc5b) — no security finding. Verified: the CSV column gate with `comment.char = ""` counts at least what either reader builds on 12 edge cases (CRLF, CR-only, BOM, indented `#`, quotes, backslash, NUL); the sigma-draw rewrite is `identical()` under a seed and stays inside the chunk bound; the scrub reaches the parse child's directory. Informational note 1: a NUL byte anywhere in a CSV now refuses the file, where the readers would have truncated or read it narrow | informational | **accepted** — refusing is the conservative direction, and no spreadsheet program writes a NUL into a CSV. |
| 2026-09-06 | **Screen 1523, note 2** — the scrub leaves the parse child's random leaf name in a reason (for example `childQ/upload.pdf`) | informational | **accepted** — a per-request random token with no reuse; nothing a caller can act on is disclosed. Replacing the whole path with the upload's name would be tidier but is not a security need. |
| 2026-09-06 | **F1 (full-surface screen 1749, 8289429..166dc5b)** — `.iaLongToWide` built the wide table one data.frame cell at a time (keys × columns `[[<-`, an n × n frame, thousands of one-row rbinds), cubic in time and quadratic in memory, and ran in-process before every downstream gate: 1,000 all-distinct (ROW, LEVEL) lines took 91 s and 219 MB; 5,000 (the API's row limit) extrapolated to three hours on the single plumber thread. Reproduced (2.1 s at 250 lines, 12.9 s at 500) | high | **fixed** — the wide table's width is counted from the raw lines and refused above `.iaMaxLevelColumns` (200, the API's column limit) before anything is built; the build is one indexed matrix and one rbind, linear in the lines (5,000 legitimate lines in well under a second); the API turns the refusal into a 422 at the validation stage; tests, and two tripwire pins (the gate exists; it precedes the build). |
| 2026-09-06 | **F2 (screen 1749)** — `deploy-production.yaml` fires on any completed R-CMD-check whose head branch is `main`, and R-CMD-check runs on `pull_request`, so a fork's pull request from a branch named `main` would run the privileged deploy on the fork's commit (its `tools/securityCheck.R` on the runner, its code to production). Verified latent: one historical fork PR from a `main` branch overlapped the workflow by two days; no such run ever fired and every deploy is an ancestor of main | high (latent) | **fixed** — the deploy job also requires `workflow_run.event == 'push'` and `head_repository.full_name == github.repository`; a tripwire assertion in group 3 requires both of any `workflow_run` workflow; AGENTS.md's "forked PRs get no secrets" now says so of the chained deploy too. |
| 2026-09-06 | **F3 (screen 1749)** — the journal-view workbook deduplicated sheet names case-sensitively, and openxlsx refuses names that differ only by case, so two trials "Trial A" and "trial a" made the Download Baseline Table handler error | low | **fixed** — the duplicate check is case-insensitive; test with two case-variant trials. |
| 2026-09-06 | **N1 (screen 1749)** — an honestly declared workbook up to `.apiMaxUncompressed` (100 MiB) is read by openxlsx in-process, not in the parse child; the screen suspected but did not measure the cost | informational | **accepted, measured** — a cap-sized 10,000 × 500 numeric workbook is 25.2 MB compressed (above the API's 25 MiB request cap, so it cannot arrive there; the app's cap is 50 MB), 170 MB uncompressed, and reads in 15 s using about 1 GB; a 100 MiB-uncompressed workbook is therefore about 9 s and 600 MB on the worker. A real baseline-table workbook is under 1 MiB, but the same cap bounds Word manuscripts, whose embedded pictures are legitimately large, so lowering it is Steve's call (a separate, lower xlsx cap would be the shape of the fix). |
| 2026-09-06 | **N2 (screen 1749)** — `.ppPageBands` allocated its coverage bins in proportion to the words' x span, so a hostile PDF placing one word far off the page would allocate gigabytes in the parse child | informational | **fixed** — the span is clamped to 20,000 bins (a generous page width in points; `binOf` already pins out-of-range positions to the last bin); test with a word at x = 10⁹. |
| 2026-09-06 | **Nightly screen 2100** (e561fb9..166dc5b, the #195 range re-screened because the 1749 row had moved into #199) — no security finding; one informational note: the sigma rewrite's "bit-identical" claim has an edge, since `rnorm()` consumes no draw when its sd is exactly 0 and the new form always does, so a row whose arms all print SD 0 shifts the seeded stream for everything after it | informational | **accepted** — the values are unchanged; only cross-build reproducibility of such a file is, and the seed is documented as build-specific (statistics.md, "the build matters as much as the seed"). The P_Calc comment now states the edge. |
| 2026-09-07 | **F1 (merged-range screen 0702, 166dc5b..7e12b13)** — sheet-name de-duplication appended " k" to a fixed 28-character base, so the hundredth of 101 trials identical in their first 31 characters produced a 32-character name and openxlsx refused the journal-view download. Reproduced | low | **fixed** — the suffix is built first and the base trimmed to fit, so a name is never longer than 31; test with 101 such trials (fails on the previous code). |
| 2026-09-07 | **I1 (screen 0702)** — the long-layout width gate bounds the wide frame's columns, not its rows; the app's sequential uploads append with no combined-row cap (pre-existing, shared by every per-line loop in the validator) | informational | **accepted** — the converter is now linear, so the range strictly improves the path; a combined-row ceiling in the append path is noted for a later decision. |
| 2026-09-07 | **I2 (screen 0702)** — the new `workflow_run` tripwire read raw YAML, so a commented-out copy of the condition would have satisfied it | informational | **fixed** — comments are stripped before the grep, as the R-side checks already do. |
| 2026-09-07 | **F1 (merged-range screen 1036, 7e12b13..df8fcb1)** — the median/IQR branch's scale draw (#202) resamples every arm before simulating it, halving its rate (6.8e6 draws per second against 1.3e7, measured), and the API's draw budget still priced a median line at one draw per subject, so a token holder's worst-case median payload cost about 30 minutes of the single thread rather than the documented 20 | low | **fixed** — `.apiDrawWork` counts a line with a quartile at twice its N; the budget's comment states the measured rate; test. The direct draw for large median rows stays on the ideas list. |
| 2026-09-07 | **F2 (screen 1036)** — sheet names could still produce a workbook Excel refuses: a leading or trailing apostrophe (a file named `'Smith 2024.pdf`), or characters outside the Basic Multilingual Plane, which Excel counts as two each against its 31 (verified against Excel through COM); openxlsx accepts both | low | **fixed** — one `sheetName()` helper strips forbidden punctuation, replaces non-BMP characters with spaces, trims, strips apostrophes at either end after every truncation (a cut can manufacture one), and never returns an empty name; tests for all five cases. |
| 2026-09-07 | **F1 (screen 1059, df8fcb1..4bceab8)** — the sheet-name helper of #204 stripped apostrophes at the ends and then trimmed whitespace, so a name ending apostrophe-space-apostrophe kept its inner apostrophe once the space went (`abc' '` gave `abc'`; reproduced), and Excel refuses that name | low | **fixed** — apostrophes and whitespace are stripped from the ends together in one pass after the truncation; the three shapes the screen listed are in the test, which failed on the previous code. |
| 2026-09-07 | **F2 (screen 1059)** — the factor of two that #204 gave a median line in the draw budget did not reproduce: measured on fully escalating median rows at 10, 50 and 5,000 per arm, the branch runs at 2.5 to 2.8 times the continuous loop's cost per subject-draw (row-sorting the resample is over half of it), so a worst-case median payload was about 28 to 37 minutes, not 20 | low | **fixed** — the weight is 3, the measured ratio rounded up (the ratio survives a change of host; the absolute rate does not); the budget's note states the measurement. The direct draw for large median rows stays on the ideas list. |
| 2026-09-07 | **Screen 1119** (4bceab8..ae81725, the #205 range) — no findings; the sheet-name and draw-weight fixes verified by running; noted, not as a finding, that the weight of 3 sits at the edge at 5,000 per arm (ratio 3.1) | — | **clean**; recorded. |
| 2026-09-07 | **I1 (screen 1441, 4bceab8..6e6077b)** — the zero-statistic snap of #207 used a tolerance of 10⁻²⁰ times the centre squared, which outgrows a printed grid step squared for means of about 10¹⁰ and above at integer printing (10⁸ at two decimals): such a row snapped every statistic to zero and reported p = 0.5 with the floor note whatever its arms said. Non-alarming direction; no baseline table plausibly holds such a variable | informational | **fixed** — the factor is 10⁻²⁶, six orders above the dust it absorbs (about 10⁻³² of the centre squared per arm) and below a grid step squared up to the validator's 10¹² ceiling at integer printing; test at means near 10¹¹ expects no floor note and a p that responds to a one-unit difference. |
| 2026-09-07 | **I2 (screen 1441)** — `.iaTieRank` grouped an infinite statistic with the largest finite one and an NA propagated to every later rank; unreachable from validated input | informational | **fixed** — non-finite gaps start their own group and non-finite comparisons are not ties, so the helpers are robust for a direct caller; test. |
| 2026-09-07 | **F1 (screen 1459, 6e6077b..3043ff9)** — the zero snap's 10⁻²⁶ tolerance (#211) no longer absorbed the floating-point dust for a thousand or more arms printing the same non-integer mean near 10⁹ or above: the replicates' N-weighted centre (Rfast row sums) carried dust the observed row (base R's sum) did not, so an honest row of that shape reported the stage floor with the note instead of p = 0.5. Alarming direction; a shape no baseline table has; introduced by #211 | low | **fixed** — both branches translate their means by the first arm's printed value before the statistic, which is translation-invariant, so identical means are exactly zero in both arithmetics at any magnitude and arm count; the snap stays as a guard. Test: the screen's failing shape (1,000 arms × 5,000, two-decimal means at 10⁹, SD 0.05) expects p = 0.5 and the note; the 1441 cases and ordinary rows unchanged. |
| 2026-09-07 | **F2 (screen 1459)** — the direct-draw chunk was sized by the fully simulated subjects only, so a row whose arms all take the direct draw (N ≥ 100, SD ≥ 3 grid steps) had a chunk of the whole stage and four 100,000 × arms matrices at the top stage: 2.3 GB at 1,000 arms, 4.5 GB at 2,000 (measured), unbounded in the app, which has no row cap; a budget-passing API request reached 2.7 GB. Pre-existing since PR #176 | medium | **fixed** — the chunk is also bounded by 10⁷ / arms, and the N-weighted centre is a matrix product rather than a ch × arms weight matrix, so each matrix stays near 80 MB and the row's peak under half a gigabyte; known answers unaffected (the chunk shrinks below the stage only above 100 arms at 100,000). Test: 400 direct-draw arms at 100,000 replicates, peak under 0.5 GB. |
| 2026-09-07 | **I1–I3 (screen 1459)** — `.iaTieCounts` with an infinite observed statistic; two infinite statistics in `.iaTieRank`; `if (obs <= zt)` on an NA statistic | informational | **accepted** — all three unreachable after validation (every input finite and below 10¹²), and a direct-call error is a 422 or a log line through both callers' tryCatch. |
| 2026-09-07 | **F1 (merged-range screen 1609, 3043ff9..bd276d3)** — the fail-safe percentage fill of #213 pushed every ambiguous arm away from ONE pooled proportion, so every ambiguous arm on the same side of it took the same endpoint: three arms printing 50% of 2,000 beside a small arm printing 52% of 60 all became 990, the identical proportions the rule exists to prevent, and an honest four-arm row of that shape read p = 0.0094 where honest draws give 0.28 to 0.88 (the screen measured it under `r2dtable`). Introduced by #213; three arms alone do not show it, because they sit exactly on the pooled proportion where the first rule already alternated | high (false alarm on an honest manuscript) | **fixed** — the ambiguous cells are split against each other: ordered by the proportion their brackets imply, the lower half take the bottom of their bracket and the upper half the top, and cells whose percentages imply the same proportion alternate; a single ambiguous cell still moves away from the pooled proportion of the rest. The screen's own four-arm reproduction is the regression test, verified to fail on #213's code and pass on the fix; the app legend, the checkbox, the flags, the two guides, statistics.md and method-history.md carry the corrected rule. |
| 2026-09-07 | **F2 (screen 1609)** — `/analyze` never returned the reader's `flags`, so the one-call route handed back an `overallP` computed from FAIL-SAFE counts, counts not printed on the manuscript page, with nothing in the reply saying so; `/parse` had always returned them | medium | **fixed** — `flags` is added to the `/analyze` success body (omitted entirely when empty, so the ordinary response is unchanged) and documented in the API guide's response table. The screen also suggested a `pctApprox` query parameter so an API caller could ask for the strict read: **accepted, not done** — Steve's decision of 2026-09-07 is that the API makes the same fail-safe choice as the app and forwards the note, and a second behaviour on the public route is a product decision, not a security need. |
| 2026-09-07 | **F3 (screen 1609)** — the conservative direction is much larger than "conservative" conveys, and is now the API default: a two-arm row of 5,000 per arm reads p = 0.008 printed as counts and p = 0.68 printed as "50%"/"50%", so publishing percentages instead of counts removes a categorical row's alarm | informational | **accepted, documented** — the rule is Steve's design decision and stands. The magnitude is now stated in statistics.md, the user guide, the API guide and the app's legend ("a row that would alarm on printed counts usually will not alarm on printed percentages"), and F2's fix makes the flags travel with the p on both routes. |
| 2026-09-07 | **F1 (merged-range screen 1654, bd276d3..9e7bf68)** — the rank-split fail-safe rule of #215 ordered only the AMBIGUOUS arms, so the exactly pinned arms had no vote and two tied pairs could be split correctly against each other while both moved toward the rest of the row. Measured over 86,310 synthetic rows: the assignment was short of the most heterogeneous consistent reading in 57.9% of rows, reported p < 0.05 where a consistent reading gives ≥ 0.05 in 0.27%, and in the worst case (five arms printing 49%, 45%, 47%, 47%, 49% at N = 3000, 40, 200, 5000, 150) built a row reading p = 0.116 against 0.872. The guarantee "less alike than the truth, never more" is asserted in six user-facing places | medium (false alarm on an honest manuscript) | **fixed** — the heuristic is replaced by a maximisation: `.ppFailsafeCounts()` enumerates one bracket end per ambiguous arm, holds the pinned arms fixed, and takes the assignment with the largest fixed-margin Pearson statistic, the statistic the engine goes on to simulate. It is convex in the counts, so the maximum is at a vertex; exhaustive up to twelve ambiguous arms (the screen's bound, since the arm count follows the document's columns), coordinate ascent from both corners beyond. The screen's worst case now builds its own conservative reading exactly; verified against exhaustive search on 3,000 random rows, and the tests call the package's helper rather than a copy of the rule, which is why the earlier copies could not fail. The six texts keep "never more" because it is now true by construction. |
| 2026-09-07 | **F2 (screen 1654)** — the comment on the single-ambiguous-arm branch said "the pooled proportion of the rest" where the code pooled included that arm's own bracket middle; no behavioural difference, since the sign is the same either way | informational | **fixed** — that branch no longer exists: the maximisation covers one ambiguous arm and many with the same code. |
| 2026-09-07 | **F3 (screen 1654)** — `/analyze`'s new `flags` echo document text (skipped line labels, arm-size snippets, and with the AI assist on, model output the manuscript can steer), unscrubbed by `.apiScrubPath` and unbounded by the row cap, which bounds the table and not the lines the reader refused | low | **fixed** — `.apiSafeFlags()` scrubs the request and parent temp directories exactly as `reasons` are scrubbed, truncates each flag at 2 KiB and the array at 50 entries, and marks every truncation; both routes use it, since `/parse` returned the same strings unscrubbed. The screen's third observation (a client that renders `flags` as HTML or feeds them to another model) is **accepted**: the strings are JSON-encoded, control characters are stripped by `.ppCleanLabel`, and the guide already tells a caller that flags quote the manuscript. `skipped` on `/parse` stays unbounded — a documented part of that route's contract, bounded in practice by the parse timeout — and is noted here rather than changed. |
| 2026-09-07 | **F1 (merged-range screen 1758, 9e7bf68..4305026)** — a precision column says what grid a printed value sits on and the engine reads that grid as the interval the value stands for, but nothing bounded the grid against the value: `ROUND_DISPERSION = -5` on a supplied spreadsheet or a posted template multiplied the null's spread by 100,000. Reproduced on an honest two-arm median row (N = 40, medians 50 and 52, quartiles 45–55 and 47–57, m = 10,000): p = 0.64 at 0, 0.187 at −3, 0.0049 at −5, and 9.999e−05 — the reportable floor — at −10 and below; the mean/SD branch carried the same lever (0.624, 0.037, 9.999e−05). The quartile draw of #214 flipped the direction of the failure from false clearance to **false accusation**, which is the dangerous one for this instrument | high (a manufactured accusation against a third party) | **fixed** — a row is refused ("The stated precision does not match the printed values") when a printed value does not sit on the grid its own precision column names, checked per column (means against `ROUND_MEAN`, SD and quartiles against `ROUND_DISPERSION`) with a tolerance proportional to the value, never to the grid. The test is consistency, not coarseness: quartiles of 40 and 60 reported to the nearest ten are analyzed, an SD of 10 reported to the nearest ten is analyzed, and the narrow-quartile case the quartile draw exists for is untouched. Tests cover every value the screen measured, in both branches, plus the honest coarse cases; the refusal is documented in statistics.md, the user guide and the API guide. **Not done, and accepted:** a matching `validateData()` issue so the app paints the cell before the run — the engine's refusal already reaches the results table and the API's `P` cell, and the validator's precision rules are due their own pass. |
| 2026-09-07 | **F2 (screen 1758)** — the fail-safe maximisation of #217 enumerated 2^k vertices, and the arm count follows the columns a document declares: 59 ms per row at twelve ambiguous arms, and a 382 KB synthetic document of such rows took 56 s against 19 s with the fill off, enough for one request to hold the single-threaded service for its whole timeout | medium (availability) | **fixed** — the enumeration bound drops from 12 to 8 (256 vertices, about 4 ms), which the screen showed costs nothing in answers: coordinate ascent matched the exhaustive optimum on 400 of 400 random rows of 13 to 16 arms. The ascent's `repeat` also gains an explicit sweep cap, so termination is a property of the code rather than of an argument about it. Test: twelve ambiguous arms return the exhaustive answer in well under the measured cost. |
| 2026-09-07 | **F3 (screen 1758)** — `/parse` bounded `flags` while returning `skipped` unbounded in the same reply: one entry per table line the reader refused, carrying the document's own text at full length, unscrubbed — the exact exposure the flags fix had just closed. The app expanded the same list into one grid row per refused line | medium | **fixed** — `.apiSafeSkipped()` caps the array at 200 entries with an explicit "…N further line(s) omitted" marker and puts every label and reason through the flags' own scrub and byte cut; the app's grid expansion is capped at `.iaMaxSkippedRows` with the remainder named in a row. Tests for both caps and for the scrub. |
| 2026-09-07 | **F4 (screen 1758)** — `.apiSafeFlags` inspects strings (`nchar`, `substr`, `gsub(fixed = TRUE)`), all of which raise on text that is not valid UTF-8, so a flag carrying such text would turn a successful parse into a 500. No reader is known to produce one | low | **fixed** — flags are put through `enc2utf8()` and an `iconv()` round trip first, and anything unreadable is replaced by "&lt;unreadable flag text removed&gt;" before the scrub and the cut. Test with an invalid byte sequence, including a valid neighbour in the same vector. |
| 2026-09-07 | **Observation (screen 1758)** — the median branch's chunk had no arm-count term, where the continuous branch was given one by screen 1459's F2; at one subject per arm its matrices reach 2.5e7 doubles against the continuous branch's deliberate 1e7 | informational | **fixed** — the median chunk takes the same `1e7 / COLS` term, so the two branches agree. |
| 2026-09-07 | **F1 (merged-range screen 1907, 4772b41..e1fbf4c)** — the precision gate added the day before tested `ROUND_MEAN` and `ROUND_DISPERSION` and not `ROUND_OBSERVATION`, the third column and the one that sets the grid every simulated observation is rounded to. A coarse value quantises the replicates while the printed values stay put, so the null's spread inflates and honest data looks impossibly alike — the same false-accusation lever screen 1758 rated high, still open through the column the fix did not name. Verified end to end: five honest arms of 40 read p = 0.433 at `ROUND_OBSERVATION = 0` and **0.0125** at −2; integer means near 50,000 read 0.586 and **0.00013** at −5, with no validation issue and no flag | high | **fixed** — a statistic built from N observations on a grid of h lies on a grid no coarser than h/N, so the interval the printed value stands for must contain a multiple of h/N, per arm (`.iaObservationGridOK()`). The test is vacuous for every ordinary table, refuses both of the screen's rows, and the refusal now names all three columns. A tripwire property in `tools/securityCheck.R` requires the gate to call all three helpers, verified to trip on a deliberate break. |
| 2026-09-07 | **F2 (screen 1907)** — zero sits on every grid, so a row whose printed values are all zero ("rescue analgesia, median (IQR): 0 (0–0)", a shape many analgesia trials carry) passed the gate at any stated precision: at `ROUND_DISPERSION = -20` its quartiles were drawn across 1e19 and the row went from its honest p = 0.5 to below 0.0001 | medium | **fixed** — a row with no scale of its own is judged on whether its precision columns agree with each other: a dispersion grid coarser than the location's is a claim no page can be making (`.iaZeroRowGridOK()`). The honest all-zero row keeps p = 0.5 and its "attainable floor" note. The screen's other observation, that a coarse grid the values genuinely sit on is benign (0.433 → 0.425), is confirmed and unchanged by design. |
| 2026-09-07 | **F3 (screen 1907)** — the app-side cap added for screen 1758's F3 built its "N further lines omitted" marker as a two-column literal and then selected the parser's three column names out of it, raising "undefined columns selected" on every document that reached the cap — so the exposure was not closed, and the file took the editor's Shiny session down instead | medium (a hostile-document denial of service introduced by the previous fix) | **fixed** — the marker row is built from the frame itself, and the capping moved into `.iaCapSkipped()` in `R/app_globals.R` so that a test can call what the app calls. |
| 2026-09-07 | **A1 (screen 1907)** — the test pinning that cap asserted only that a constant existed and was numeric; nothing in `tests/` touched the capping code, so the suite was green while the code raised on its first real input | assurance | **fixed** — replaced by a test that calls `.iaCapSkipped()` on a genuine three-column frame of 250 rows and checks the row count, the column names, the last kept row and the marker text; verified to fail on the code it pins. The same lesson as the fail-safe rule's two failed versions: a test that restates the code cannot disagree with it. |
| 2026-09-07 | **F1 (merged-range screen 2000, e1fbf4c..450407b)** — the observation-grid rule added the hour before is vacuous wherever hObs/N is finer than the printed mean's own step, which an attacker arranges by choosing N (at 1,000 per arm and integer means, an observation grid of 1,000 passes unconditionally), and nothing checked the printed SD against that grid at all. Verified: three arms of 1,000 printing mean 500 and SD 1 read p = 0.5 honestly and **9.999e−05 with the "attainable floor" note** at `ROUND_OBSERVATION = -3`; two arms of 100 at mean 50, SD 1 read 0.02855 at −2 | high (a manufactured accusation, third occurrence of this lever) | **fixed** — the dispersion side is a theorem, not a heuristic: N values on a grid of width h have a sample SD that is exactly 0 or at least h/√N, so a row is refused when the printed SD, taken at the top of its own printed interval, falls below that bound. It can only refuse tables no rounded sample could produce. All four of the screen's rows are refused, every honest and pinned shape still runs, and the median branch needs no analogue (its divisor of 1 or 2 already holds hObs to within a factor of two of the median's own grid). |
| 2026-09-07 | **F2 (screen 2000)** — every grid test is one-sided by construction: a decimal sits on every finer grid, so a stated precision FINER than the printed value passed all of them. An over-fine `ROUND_MEAN` erases the rounding of the simulated arm means, and that rounding is the tie mass by which an honest table with identical printed means earns a large p. Verified: three arms of 30 printing mean 0.5 read 0.4185 honestly and **9.999e−05** at `ROUND_MEAN = 15`; mean 50 read 0.01455 and 9.999e−05 at 13. The screen measured and REJECTED the same claim for `ROUND_DISPERSION` (0.07515 → 0.07615) and for an over-fine `ROUND_OBSERVATION` alone (0.0098 → 0.00995) — the lever is `ROUND_MEAN` | high | **fixed by disclosure, and the reasoning matters** — refusing was implemented first and then rejected: a mean stored as 50 may have been printed "50" or "50.000000", since a spreadsheet keeps no trailing zeros, so the honest row and the manipulated one are the same numbers. The 2026-09-06 outside audit's own case is the honest one (integer observations beside a six-decimal printed mean, where the small p is the right answer), and a refusal would have thrown it out — its test caught exactly that. So the row is analysed as the table claims and the claim rides with the answer: a Note says "the stated mean precision (k decimals) is finer than the printed values; the p depends on that claim", on the rows where it decides anything (the printed means all equal — a printed unit apart the screen measured the effect as negligible). The screen's other suggestion, a `validateData()` cell issue for a supplied `ROUND_MEAN` that differs from the value's own decimals, is **accepted, not done**: it belongs with the validator's own precision pass. |
| 2026-09-07 | **F3 (screen 2000)** — the skipped-line cap was applied at one of its two call sites (the wide-spreadsheet branch never called it, and its skips are bounded only by the 10,000-row sheet cap), and the registry it feeds was consumed by a loop that re-coerced both grid columns per entry and scanned the whole frame: measured 45 s per grid render at 60,000 entries and 60,000 rows — one zip of 300 documents, every file inside its own per-file cap — on a single-threaded shared server, repeated on every edit | medium (availability, against every user of the deployed instance) | **fixed** — the loop builds its key once and finds rows by `split()`/lookup, which is linear in entries plus rows; the wide branch calls `.iaCapSkipped()` too; a tripwire property requires both call sites to cap, and a test asserts it. **Accepted, not done:** a session-wide cap across uploads, as the screen also recommends — the registry accumulating across uploads is pre-existing and bounded per file, and a session ceiling is a product decision about what an editor may assemble in one sitting rather than a defect of this range. |
| 2026-09-08 | **F1 (merged-range screen 2101, 450407b..224a9db)** — the dispersion bound used the weakest form of its own theorem: alpha replaced by its minimum feasible value 1/√N, leaving a factor of √N/2 of headroom exactly at the operating point the earlier screen had named, a printed mean near a half-grid point. Verified: three arms of 1,000 printing means 500/501/499 with SD 40 read 0.414 honestly and **0.00235** at `ROUND_OBSERVATION = -3`; identical means read the reportable floor at SD 40 and at SD 100. Every one of those tables is arithmetically impossible — a mean of 500 on a grid of 1,000 forces SD ≥ 500 — and every one passed the bound of 31.6 | high (fourth occurrence of this lever) | **fixed** — the bound is the sharp statement, `SD ≥ h · alpha` with alpha the distance from the mean to the nearest multiple of the grid, minimised over the printed mean's own interval so no honest table is refused for the width of its printing, and floored at 1/√N. All six of the screen's rows are refused; the honest row is analysed. Pinned in `tools/securityCheck.R` beside the other three helpers. |
| 2026-09-08 | **F5 (screen 2101)** — the same bound refused ordinarily printed honest rows, because a blank `ROUND_OBSERVATION` defaults to `ROUND_MEAN`, which is a guess ("a mean printed to d decimals means the observations lie on a grid of 10^-d") and false for any continuous variable whose mean happens to print without decimals. Verified: "2 ± 0.2" at eight patients, with **no precision columns supplied at all**, was refused — and a refused row leaves the trial silently, so the Summary reported a p from two of three rows with nothing saying so. The second consequence is worse than the first: an arithmetically impossible row, the strongest signal the instrument has, was being silenced instead of reported | medium (a regression introduced by the previous range) | **fixed, both halves** — the bound applies only where the table STATES an observation grid coarser than the printed value's own; where they agree the quantisation it is about cannot arise. And the Summary line now carries "k of n rows analysed" whenever any row was refused, for every refusal reason, so a combined p is never read as covering a table it did not cover. |
| 2026-09-08 | **F2 (screen 2101)** — the fine-precision disclosure accepted two decimals of slack, and the row was already at the reportable floor inside it: printed means of 0.5 with `ROUND_MEAN = 2` read 0.01335 and with 3 read 9.999e−05, **with no note at all** | high | **fixed** — the slack is zero. A note is the cheap side of an error: one on an honest row that lost a trailing zero costs nothing, one missing from a manipulated row costs an accusation. Pinned at one, two and fifteen decimals past the printed digits. |
| 2026-09-08 | **F3 (screen 2101)** — the loop the previous range recorded as linear was still quadratic: `bySk[[key]]` is a linear name search, because R hashes environments and not list names, and the payload grew one `[[<-` at a time. Measured side by side: 0.09 s against 0.28 s at 2,000, but **28.8 s at 40,000**, a constant factor of 1.6 rather than a change of complexity, and the log recorded the property as fixed when it was not | medium (availability) | **fixed** — the mapping is `.iaSkipPayload()`, one `match()` and one bulk assignment, and it is a function so a test can measure it rather than a grep asserting it. The test compares 2,000 against 16,000 rows and requires the larger to cost well under a linear-times-24 budget; on this machine it is 0.06 s. |
| 2026-09-08 | **F4 (screen 2101)** — the wide-sheet cap was applied per trial BLOCK, and `parseWideTable()` returns one block per "Trial:" marker with no cap on the markers, so a 10,000-row sheet of three-row blocks was bounded at 200 × 3,300 — that is, not bounded. The screen also predicted that the tripwire pinning the old call site would trip on the correct fix | low-medium | **fixed** — `.iaCapSkippedFile()` spends one budget across a file's blocks, the block that exhausts it carries the marker and the rest contribute nothing; the tripwire now pins that call site, and a test caps 3,300 blocks at the file's budget. |
| 2026-09-08 | **I6 (screen 2101)** — the median row's Note filtered the skew note for emptiness but not the new precision note, leaving a trailing "; " in the results table, the workbook and the API | informational | **fixed** — both are filtered; test. |
| 2026-09-08 | **F1 (merged-range screen 2241, 224a9db..b03709e)** — the fine-precision Note, which screen 2000 adjudicated as the WHOLE remedy for that finding (refusal having been considered and rejected), was switched off by perturbing one arm's mean by a single unit in the last place. The filter compared raw doubles, so `0.5, 0.5 + 2⁻⁵², 0.5` with `ROUND_MEAN = 15` read the reportable floor with no note at all — and the perturbation is invisible to `format(digits = 15)`, to `.iaDecimals()` and to the grid | medium-high (a remedy the manuscript can switch off) | **fixed** — the arms are compared at the precision their values carry, `round(v, shown)`, the same quantity `.iaStatedPrecisionNotTooFine()` already computes. Pinned at a one-ulp perturbation, with the test asserting that the engine's own view of the two values is identical. |
| 2026-09-08 | **F2 (screen 2241)** — every guard added this week points at the ACCUSATION direction. A precision stated *coarser* than the printed digits drives the p the other way, by widening the rounding the arms are judged against, and was neither refused nor disclosed: three arms of 100 printing an integer 50 with SD 30 read p = 9.999e−05 at `ROUND_MEAN = 1` and **0.275** at −1; a median row's `ROUND_OBSERVATION` took p = 0.0557 to **0.5** at −1. It is the direction an author benefits from | medium (a false negative, undisclosed) | **fixed by disclosure**, symmetric with screen 2000's F2 and for the same reason: a paper printing "50" may honestly have rounded to tens, so the claim cannot be refused, but it can be said. A Note now names a stated precision coarser than the digits the values carry, on the rows where it decides the answer. **Accepted, open, and a decision for Steve Shafer:** the screen also asks for a dispersion-side consistency test on the median branch — the printed quartiles must be reachable as type-7 quantiles of N values on the stated observation lattice. That is a new REFUSAL, and the last two screens each showed a new refusal throwing out honest rows; it wants its own measurement on the corpus before it goes in. |
| 2026-09-08 | **F3 (screen 2241)** — the derived-cell payload loop was the same quadratic the skip registry's had just been rewritten to remove, forty lines above it in the same reactive: two whole-column coercions per registry entry and one `[[<-` by name per painted cell. Measured on a verbatim replication: 7.8 s for 25,000 painted keys, 31.4 s for 50,000, **191.9 s for 100,000**. And unlike the skips, the derived registry had **no cap anywhere** and accumulates across uploads | medium (availability, against every user of the shared instance) | **fixed** — `.iaDerivedPayload()` beside `.iaSkipPayload()`, one pass and one bulk assignment, with the same last-entry-wins semantics the per-cell overwrite had; both registries are now capped for a session at `.iaMaxRegistryRows`, which drops only paint and never a number. Tests: the mapping's answers on a small frame, and 20,000 painted keys inside a five-second budget. |
| 2026-09-08 | **F4 (screen 2241)** — `.iaCapSkippedFile()` recognised its own marker rows by a `"..."` prefix on the LABEL, which is the document's own text: a sheet row labelled "... continued" was filtered out of the frame and mis-counted | low (display integrity) | **fixed** — a marker is recognised by its `reason`, which the parser writes from its own vocabulary and no document can set, through one `.iaIsSkipMarker()` helper. Test with a decoy row inside the kept range. |
| 2026-09-08 | **F1 (merged-range screen 2339, b03709e..e900933)** — the fine-precision Note, the whole adjudicated remedy for that shape, was still switchable off and more cheaply than before: both definitions of "the arms are equal" were computed from the same attacker-supplied doubles, so widening the perturbation by one decimal defeated them. Verified: at 1e−15, 1e−14 and 1e−13 the p stayed bitwise identical at the reportable floor and the note vanished; at 1e−12 the "attainable floor" note went too. The engine's own definition of equality is a tolerance (the zero snap) and the note's was exact, and the manipulation sat in the gap | medium-high | **fixed** — the note now fires on EITHER of two tests, so both must be defeated: the engine's own verdict (the observed statistic snapped to zero, passed in from the branch that computes it) and equality at the FEWEST decimals any arm carries. The second also closes the first gate's hole, where giving one arm fifteen decimals raised the bar the stated precision was measured against. Pinned at seven perturbations from zero to 1e−11. |
| 2026-09-08 | **F2 (screen 2339)** — the coarse-precision note added an hour earlier fired on the configuration the user guide's own worked example teaches (integer observations beside a one-decimal mean, `docs/user-guide.md`'s "when you know the raw precision, say so"), and asserted that "a large p here rests on that claim" on rows reading p = 0.005, where the claim had moved the p by nothing to three significant figures | low-medium (a false causal statement on honest rows, and a channel an author could spray with noise) | **fixed** — `ROUND_OBSERVATION` is dropped from the note, which describes the MEAN's own grid; the wording no longer asserts the p is large. The observation grid's effect on a median row is real and remains undisclosed: it is part of the dispersion-side test left open for Steve Shafer, and is named there. |
| 2026-09-08 | **F3 (screen 2339)** — `.iaDerivedPayload()` still scanned the whole grid once per registry entry: the first of the two quadratics the earlier finding named, with only the registry capped and the grid unbounded. Measured against its sibling on the same data: 0.92 s against 0.017 s at 10,000 grid rows, 7.43 s against 0.028 s at 100,000. The log had recorded the property as "one pass", which it was not | low (a residual; at that grid size the table's own serialisation dominates) | **fixed** — the addressed entries are found by one `match()` as the sibling does, and only the whole-trial entries keep a loop, of which there is one per OCR-read or arm-N-recovered file. The pinning test now holds the registry at its cap and scales the GRID, the dimension nothing bounds. |
| 2026-09-08 | **F4 (screen 2339)** — `.iaCapRegistry()` truncated head-first, and these registries accumulate in upload order, so an early file in a zip could push a later table's OCR warning paint out of the grid — the paint that tells the editor a table was read by optical character recognition | low | **fixed** — the newest rows survive. Test: a nine-thousand-row registry whose last entry is the OCR warning keeps it. |
